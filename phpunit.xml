<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         failOnWarning="true"
         failOnRisky="true">
    <!-- workshop: the infra-free unit suite (composer test). integration: runs the
         real commands against the live compose stack (kafka + schema-registry +
         mysql); gated behind KAFKA_INTEGRATION=1 and driven by `make integration`. -->
    <testsuites>
        <testsuite name="workshop">
            <directory>tests</directory>
            <exclude>tests/Integration</exclude>
        </testsuite>
        <testsuite name="integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <!-- ignoreIndirectDeprecations: drop deprecations triggered in third-party
         code (e.g. the schema-registry client under PHP 8.4) — outside our control
         and already silenced at runtime in bin/console. Deprecations in our own
         src/ still surface and would fail the suite. -->
    <source ignoreIndirectDeprecations="true">
        <include>
            <directory>src</directory>
        </include>
    </source>
</phpunit>
