#!/usr/bin/env bash
set -euo pipefail

if [[ $# -lt 1 ]]; then
  echo "usage: bin/topic-describe <topic>" >&2
  exit 2
fi

docker compose exec kafka kafka-topics \
  --bootstrap-server kafka:29092 \
  --describe --topic "$1"
