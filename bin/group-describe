#!/usr/bin/env bash
set -euo pipefail

if [[ $# -lt 1 ]]; then
  echo "usage: bin/group-describe <group-id>" >&2
  exit 2
fi

docker compose exec kafka kafka-consumer-groups \
  --bootstrap-server kafka:29092 \
  --group "$1" \
  --describe
