# Workshop defaults — committed. Override per user via .env.local (gitignored).

APP_ENV=dev
KAFKA_BROKERS=kafka:29092
SCHEMA_REGISTRY_URL=http://schema-registry:8081
DATABASE_URL=mysql://workshop:workshop@mysql:3306/workshop?charset=utf8mb4
